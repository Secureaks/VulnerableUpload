<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Vulnerable Upload Application{% endblock %}</title>

        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}

        <div class="bottom-buttons">
            <a href="{{ path('app_profile', {'example': 1}) }}" class="{% if example == 1 %}selected{% endif %}">Example 1</a>
            <a href="{{ path('app_profile', {'example': 2}) }}" class="{% if example == 2 %}selected{% endif %}">Example 2</a>
            <a href="{{ path('app_profile', {'example': 3}) }}" class="{% if example == 3 %}selected{% endif %}">Example 3</a>
            <a href="{{ path('app_profile', {'example': 4}) }}" class="{% if example == 4 %}selected{% endif %}">Example 4</a>
            <a href="{{ path('app_profile', {'example': 5}) }}" class="{% if example == 5 %}selected{% endif %}">Example 5</a>
            <a href="{{ path('app_profile', {'example': 6}) }}" class="{% if example == 6 %}selected{% endif %}">Secure way</a>
        </div>

        <div class="warning">
            This application is intentionally vulnerable to demonstrate how to exploit file upload vulnerabilities. Do not deploy it in a public environment.
        </div>
    </body>
</html>
